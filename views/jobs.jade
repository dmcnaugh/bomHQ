h3 Jobs

table.table
    tr
        th Name
        th Running?
        th Time
        th in (sec.)
        th Cron
    tr(ng-repeat="row in jobs")
        td {{row.name}}
        td {{row.running}}
        td {{row.at}}
        td {{row.timeout}}
        td {{row.cronTime}}